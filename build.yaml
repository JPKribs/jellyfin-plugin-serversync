---
name: "Server Sync"
guid: "ebd650b5-6f4c-4ccb-b10d-23dffb3a7286"
imageUrl: "https://raw.githubusercontent.com/JPKribs/jellyfin-plugin-serversync/refs/heads/master/Jellyfin.Plugin.ServerSync/Assets/Logo.png"
version: "10.11.65.1"
targetAbi: "10.11.0.0"
framework: "net9.0"
overview: "Sync media content from a source Jellyfin server to this server."
description: >
  Server Sync enables one-way content synchronization from a source Jellyfin server to a local server.
  The plugin periodically scans the source server for media items (Movies, Episodes, Audio, Video)
  and downloads missing or updated content.
category: "Administration"
owner: "JPKribs"
artifacts:
- "Jellyfin.Plugin.ServerSync.dll"
- "Jellyfin.Sdk.dll"
- "Microsoft.Kiota.Abstractions.dll"
- "Microsoft.Kiota.Http.HttpClientLibrary.dll"
- "Microsoft.Kiota.Serialization.Form.dll"
- "Microsoft.Kiota.Serialization.Json.dll"
- "Microsoft.Kiota.Serialization.Multipart.dll"
- "Microsoft.Kiota.Serialization.Text.dll"
- "Std.UriTemplate.dll"
changelog:
  developer:
  - Fixed the settings page refusing to expand its categories when this server is hosted under a base url such as /jellyfin
  - Items scheduled for deletion are no longer dropped from tracking when a refresh runs before the sync that would delete them
  - Fixed source paths mapping into the wrong local folder when one library root is a name prefix of another
  - Rejected source server URLs now explain why instead of failing with a server error
  - Source server URLs written as an IPv6 literal are now checked against the same address rules as everything else
  - Cancelling a refresh no longer records a false connection failure on the dashboard
  - A bandwidth schedule with the same start and end hour now applies all day instead of never
  - Retrying an errored item from the table gives it a full set of attempts again
  - Recycling bin file names now use a fixed calendar so retention works on every locale
  - Nested provider values no longer resolve to a type name instead of an actual value
  - Source server responses are always released now, even when cleanup itself fails
  - Cleaned up static analysis findings across the source client, the table managers, and the plugin entry point
